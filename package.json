{
  "name": "deonchingwe-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "postbuild": "node cachebust.mjs && node -e \"const fs=require('fs');fs.rmSync('out',{recursive:true,force:true});fs.cpSync('dist','out',{recursive:true})\""
  }
}
